# Copiar a .env y ajustar según el entorno

# Autenticación (NextAuth)
# Generar con: openssl rand -base64 32
AUTH_SECRET=
# URL de la app (obligatorio para NextAuth: cookies y redirects). En prod usar la URL real.
NEXTAUTH_URL=http://localhost:3000

# Base de datos PostgreSQL

# Opción 1: Contenedor Docker (docker compose up -d)
DATABASE_URL=postgresql://postgres:gestor_dev@localhost:5432/gestor_impuestos

# Opción 2: PostgreSQL local (ej. Homebrew en macOS). Crea la BD: createdb gestor_impuestos
# Reemplaza TU_USUARIO por tu usuario del sistema (ej. quienami en terminal)
# DATABASE_URL=postgresql://TU_USUARIO@localhost:5432/gestor_impuestos

# Resend (notificaciones por correo)
# Obtén tu API key en https://resend.com/api-keys
RESEND_API_KEY=
# Opcional: remitente (por defecto onboarding@resend.dev para pruebas)
# NOTIFICACION_FROM_EMAIL=notificaciones@tudominio.com
# NOTIFICACION_FROM_NAME=Gestor de Impuestos
